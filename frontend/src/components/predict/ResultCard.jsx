import Badge from '../ui/Badge';
import { formatCompactINR, formatINR, formatNumber } from '../../utils/format';
import { Link } from "react-router-dom";
/**
 * Premium prediction result card.
 * Everything shown is real API output: the price comes from the trained
 * pipeline and the metric chips come from the model's actual test-set scores
 * (no invented confidence percentages).
 */
export default function ResultCard({ result, inputsConfig, onReset }) {
  const { predicted_price: price, currency, model, performance, inputs } = result;

  return (
    <section aria-labelledby="result-heading" className="glass-card relative overflow-hidden p-6 sm:p-8">
      {/* subtle success glow */}
      <div className="pointer-events-none absolute inset-x-0 -top-24 h-48 bg-gradient-to-b from-emerald-500/15 to-transparent" aria-hidden="true" />

      <div className="relative">
        <div className="flex flex-wrap items-center justify-between gap-3">
          <div className="flex items-center gap-3">
            <span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-emerald-500/15 text-emerald-300" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className="h-5 w-5">
                <path d="M3 11l9-8 9 8M5 9.5V21h14V9.5" />
              </svg>
            </span>
            <div>
              <h2 id="result-heading" className="text-lg font-semibold text-white">
                Estimated Property Price
              </h2>
              <p className="text-xs text-slate-400">Generated by the trained {model} pipeline</p>
            </div>
          </div>
          <Badge variant="emerald">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" className="h-3 w-3" aria-hidden="true">
              <path d="M5 13l4 4L19 7" />
            </svg>
            Prediction ready
          </Badge>
        </div>

        <div className="mt-6 rounded-2xl border border-emerald-400/20 bg-emerald-500/[0.06] p-6 text-center">
          <p className="text-xs font-semibold uppercase tracking-[0.2em] text-emerald-300/90">Market Value Estimate</p>
          <p className="mt-2 text-4xl font-extrabold tracking-tight text-white sm:text-5xl">
            {formatINR(price)}
          </p>
          <p className="mt-2 text-xs text-slate-400">
            {currency} · ≈ {formatCompactINR(price)} · {formatNumber(price, { maximumFractionDigits: 0 })} rupees
          </p>
        </div>

        {/* Legitimate metric context — real test-set scores, no fake confidence */}
        <div className="mt-5 flex flex-wrap items-center gap-2">
          <Badge variant="sky">Model: {model}</Badge>
          <Badge variant="slate">Test R² = {Number(performance.r2).toFixed(3)}</Badge>
          <Badge variant="slate">Avg error (MAE) ≈ {formatCompactINR(performance.mae)}</Badge>
          <Badge variant="slate">RMSE ≈ {formatCompactINR(performance.rmse)}</Badge>
        </div>
        <p className="mt-2 text-xs leading-relaxed text-slate-500">
          Accuracy figures are the serving model's scores on the held-out test set — they describe typical model error,
          not a guarantee for this specific property.
        </p>

        {/* Input summary */}
        <div className="mt-6 border-t border-white/10 pt-5">
          <h3 className="text-xs font-semibold uppercase tracking-wider text-slate-500">Property details used</h3>
          <dl className="mt-3 grid grid-cols-2 gap-x-4 gap-y-2.5 sm:grid-cols-4">
            {inputsConfig.map((field) => (
              <div key={field.name} className="rounded-xl border border-white/5 bg-white/[0.03] px-3 py-2">
                <dt className="text-[11px] text-slate-500">{field.label}</dt>
                <dd className="text-sm font-semibold text-slate-100">
                  {inputs[field.name]} {field.unit || ''}
                </dd>
              </div>
            ))}
          </dl>
        </div>

        <div className="mt-7 flex flex-col gap-3 sm:flex-row">
          <button type="button" onClick={onReset} className="btn-primary flex-1">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" className="h-4 w-4" aria-hidden="true">
              <path d="M12 5v14m-7-7h14" />
            </svg>
            Predict Another Property
          </button>
          <Link to="/analytics" className="btn-secondary flex-1">
            View Analytics
          </Link>
        </div>
      </div>
    </section>
  );
}
